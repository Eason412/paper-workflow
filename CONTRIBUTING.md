# 贡献指南

## 变更范围

每个 PR 聚焦一个问题。文献获取与报告排版分别维护，公共安装、根 README 和 CI 由总仓库统一管理。源码、测试及使用说明应同步更新。

## 问题复现

提供系统与相关工具版本、最小输入、执行命令、预期结果、实际结果和脱敏错误。文献获取问题优先使用模拟响应或公开元数据；报告排版问题提供最小 Markdown。

不得上传浏览器 profile、Cookie、令牌、学校账号密码、验证码、受限全文或含个人信息的报告。

## 解决思路与验证

说明根因、修改范围及现有行为影响，补充对应回归测试。两套测试分别在各自 Skill 根目录运行；机构访问测试使用替身，真实登录和下载需另行授权。排版、图片、表格或公式变更应补充 Pandoc 验证，并在有编译环境时检查实际 PDF。

文献获取的中英文 README 应保持命令、参数、状态和限制一致。来源目录迁移不得改变用户的下载目录、配置或登录会话。

PR 标题建议采用 `fix:`、`feat:`、`docs:` 或 `test:` 加简洁说明。新增提交使用 GitHub noreply 邮箱，并保留原有许可证与第三方声明。
