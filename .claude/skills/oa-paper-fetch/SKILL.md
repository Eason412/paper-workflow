---
name: oa-paper-fetch
description: Execute resumable OA-first paper acquisition with optional authorized institutional browser sessions using this repository's canonical paper-fetch Skill.
---

Read `${CLAUDE_SKILL_DIR}/../../../skills/oa-paper-fetch/SKILL.md` completely and follow its canonical workflow and safety contract. Execute only the backends in that directory for `$ARGUMENTS`. Keep browser login and credentials under the canonical user-authorization rules.
