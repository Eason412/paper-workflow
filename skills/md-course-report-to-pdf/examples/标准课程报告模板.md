# 课程报告题目

> 使用说明：复制本模板后，把示例文字替换成自己的报告内容。正式转换前，请删除不需要的提示语，并确认图片文件真实存在。
>
> 基本规则：全文只保留一个 `#` 作为题目；正文从 `##` 开始；章节标题不要手写 `1.`、`1.1`；图片题和表题不要手写 `图 1`、`表 1`，模板会自动编号。

## 摘要

这里写中文摘要。建议按“研究背景、主要内容、分析方法、主要结论”的顺序写成一段完整文字，不要只写项目符号。

关键词：关键词一；关键词二；关键词三

## Abstract

Write the English abstract here. It should correspond to the Chinese abstract and briefly cover the background, method, analysis, and conclusion.

Keywords: keyword one; keyword two; keyword three

## 引言

这里写研究背景、问题来源、课程要求、报告目的和全文结构。正文可以引用参考文献，例如已有研究指出该问题具有进一步分析价值[1]。

## 背景与理论基础

这里写概念解释、理论基础、技术路线、政策背景或课程相关知识。正文应使用连续段落，不建议直接粘贴零散提纲。

### 图片写法

图片建议统一放在报告项目目录下的 `image/` 文件夹中，并使用相对路径引用。推荐目录结构如下：

```text
课程报告.md
image/
  fig_01_method_flow.png
  fig_02_result_comparison.png
  fig_03_policy_timeline.png
```

图片文件名建议使用英文小写、数字和下划线，并按正文出现顺序编号，例如：

- `fig_01_method_flow.png`
- `fig_02_result_comparison.png`
- `fig_03_policy_timeline.png`

不建议使用带空格、括号、中文编号或临时版本号的文件名，例如“图1.png”“第1张 图（最终版）.png”“figure final final.png”。

复制下面这一行到正文需要插图的位置，并把文件名替换成真实存在的图片文件名。如果暂时没有图片，就不要加入图片语法，否则转换时会提示图片缺失。

```markdown
![方法流程图](image/fig_01_method_flow.png)
```

图片题只写纯标题，不写“图 1”。不要使用绝对路径、远程 URL、`../` 跳出项目目录的路径，也不要用 HTML `<img>` 标签。

## 关键问题分析

这里写主体分析内容，可以按分析维度设置 `###` 小节。正文中第一次引用文献的位置要有实际意义，例如“相关报告显示该路线仍受成本和基础设施约束[2]”。

### 表格写法

表格使用 Markdown pipe table。表题必须紧贴表格下一行，中间不要空行；表题写成 `: 标题`，不要写 `表: 标题`，也不要写 `: 表 1 标题`。

| 指标 | 方案 A | 方案 B | 说明 |
|:---|---:|---:|:---:|
| 成本 | 低 | 高 | 相对判断 |
| 效率 | 高 | 中 | 居中示例 |
| 可实施性 | 较高 | 一般 | 结合条件判断 |
: 方案对比

对齐规则：`:---` 表示左对齐，`---:` 表示右对齐，`:---:` 表示居中。文本列一般左对齐，数字列一般右对齐。每一行列数必须一致，表头不要写太长，长解释放在表前或表后正文中。

### 公式写法

普通展示公式使用 LaTeX 数学语法：

$$
E = mc^2
$$

式中，`E` 表示能量，`m` 表示质量，`c` 表示光速。不要在公式里手写“式（1）”或“（1）”，模板会自动生成公式编号。

多行推导可以使用 `align` 环境，不要把 `align` 再包进 `$$...$$`：

\begin{align}
C_{\mathrm{total}} &= C_{\mathrm{fuel}} + C_{\mathrm{transport}} \\
\eta &= \frac{E_{\mathrm{out}}}{E_{\mathrm{in}}}
\end{align}

## 案例或数据分析

这里写案例、数据、对比结果或课程材料分析。若使用 AI 生成的图片、表格或数据，请在正文中说明来源、假设和人工核对情况[3]。

## 讨论

这里写对比分析、局限性、风险、不确定性或改进建议。不要保留 AI 提示词、画图 prompt、内部备注或“这里放图”这类占位文字。

## 结论

这里写报告总结和后续建议。结论应回扣引言中的问题，避免只重复正文标题。

## 参考文献

正文中的 `[1]`、`[2]`、`[3]` 必须和下面的参考文献编号一一对应。不要编造作者、年份、期刊、DOI 或 URL；如果学校没有强制要求，参考文献列表中尽量不要保留很长的裸 URL。

[1] 作者. 文献题名[J]. 期刊名, 年份, 卷(期): 页码.

[2] 机构. 报告题名[R]. 出版地: 出版者, 年份.

[3] 机构. 网页题名[EB/OL]. 发布日期.
